<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="ModernDesign.MVVM.View.leuFastWelcomeWindow"
        Title="Leuan | DLC Installation Tutorial" 
        Height="700" 
        Width="1100"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen" 
        Icon="/Icono_TLK.ico">

    <Window.Resources>
        <!-- Card Button Style -->
        <Style x:Key="CardButton" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="#15FFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="bg" Background="{TemplateBinding Background}" 
                                CornerRadius="20" Padding="30" BorderBrush="#3EC7E8" BorderThickness="2">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.3" Color="#3EC7E8"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#25FFFFFF"/>
                                <Setter TargetName="bg" Property="BorderBrush" Value="#0A6BF2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40" ResizeBorderThickness="6" 
                            CornerRadius="0" GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Border CornerRadius="20">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0F172A" Offset="0"/>
                <GradientStop Color="#1E293B" Offset="0.5"/>
                <GradientStop Color="#0F172A" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect BlurRadius="40" ShadowDepth="0" Opacity="0.6"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <Border Grid.Row="0" Padding="25,15">
                <Grid>
                    <TextBlock Text="🎮  Leuan's Toolkit" Foreground="White" FontSize="18" 
                               FontWeight="SemiBold" VerticalAlignment="Center"/>

                    <Button x:Name="CloseBtn" HorizontalAlignment="Right" Width="40" Height="40"
                            Background="Transparent" Foreground="White" BorderThickness="0" 
                            Cursor="Hand" Click="CloseBtn_Click"
                            WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button.Template>
                            <ControlTemplate TargetType="{x:Type Button}">
                                <Border x:Name="bg" Background="Transparent" CornerRadius="10" Padding="8">
                                    <TextBlock Text="✕" FontSize="18" FontWeight="Bold"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bg" Property="Background" Value="#EF4444"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <!-- MAIN CONTENT -->
            <Grid Grid.Row="1" Margin="50">
                <!-- Welcome Screen -->
                <StackPanel x:Name="WelcomePanel" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <!-- Animated Circle Icon -->
                    <Border x:Name="WelcomeCircle" Width="120" Height="120" 
                            Background="#3EC7E8" CornerRadius="60"
                            HorizontalAlignment="Center" Margin="0,0,0,30">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.6" Color="#3EC7E8"/>
                        </Border.Effect>
                        <TextBlock Text="🎯" FontSize="60" 
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <!-- Welcome Title -->
                    <TextBlock x:Name="WelcomeTitle" 
                               Text="Welcome to DLC Installation!" 
                               Foreground="White" FontSize="36" FontWeight="Bold"
                               HorizontalAlignment="Center" Margin="0,0,0,15"
                               TextAlignment="Center"/>

                    <TextBlock x:Name="WelcomeSubtitle" 
                               Text="What would you like to do?" 
                               Foreground="#94A3B8" FontSize="20"
                               HorizontalAlignment="Center" Margin="0,0,0,50"
                               TextAlignment="Center"/>

                    <!-- Options Grid -->
                    <Grid x:Name="OptionsGrid" Width="900">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Option 1: Install All DLCs -->
                        <Button x:Name="InstallAllBtn" Grid.Column="0" 
                                Style="{StaticResource CardButton}"
                                Click="InstallAllBtn_Click"
                                Margin="0,0,15,0"
                                WindowChrome.IsHitTestVisibleInChrome="True">
                            <StackPanel>
                                <TextBlock Text="📦" FontSize="50" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                                <TextBlock x:Name="InstallAllTitle" Text="Install All DLCs" 
                                           Foreground="White" FontSize="16" FontWeight="Bold"
                                           HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"/>
                                <TextBlock x:Name="InstallAllDesc" Text="Complete package with everything" 
                                           Foreground="#94A3B8" FontSize="12" Margin="0,8,0,0"
                                           HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <!-- Option 2: Install Some DLCs -->
                        <Button x:Name="InstallSomeBtn" Grid.Column="1" 
                                Style="{StaticResource CardButton}"
                                Click="InstallSomeBtn_Click"
                                Margin="7.5,0,7.5,0"
                                WindowChrome.IsHitTestVisibleInChrome="True">
                            <StackPanel>
                                <TextBlock Text="🎲" FontSize="50" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                                <TextBlock x:Name="InstallSomeTitle" Text="Install Some DLCs" 
                                           Foreground="White" FontSize="16" FontWeight="Bold"
                                           HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"/>
                                <TextBlock x:Name="InstallSomeDesc" Text="Choose specific content" 
                                           Foreground="#94A3B8" FontSize="12" Margin="0,8,0,0"
                                           HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <!-- Option 3: Install Base Game + DLCs -->
                        <Button x:Name="InstallBaseBtn" Grid.Column="2" 
                                Style="{StaticResource CardButton}"
                                Click="InstallBaseBtn_Click"
                                Margin="15,0,0,0"
                                WindowChrome.IsHitTestVisibleInChrome="True">
                            <StackPanel>
                                <TextBlock Text="🎮" FontSize="50" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                                <TextBlock x:Name="InstallBaseTitle" Text="Base Game + DLCs" 
                                           Foreground="White" FontSize="16" FontWeight="Bold"
                                           HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"/>
                                <TextBlock x:Name="InstallBaseDesc" Text="Full installation from scratch" 
                                           Foreground="#94A3B8" FontSize="12" Margin="0,8,0,0"
                                           HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>