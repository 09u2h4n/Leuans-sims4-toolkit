<Window x:Class="ModernDesign.MVVM.View.GalleryManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Gallery Manager - Sims 4 Screenshots"
        Width="1000"
        Height="700"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        MinWidth="800"
        MinHeight="600"
        Loaded="Window_Loaded">

    <Window.Resources>
        <Style x:Key="TitleTextBlock" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#F9FAFB"/>
            <Setter Property="FontFamily" Value="Bahnschrift Light"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontStyle" Value="Italic"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
            <Setter Property="TextOptions.TextRenderingMode" Value="Auto"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="1" BlurRadius="8" Opacity="0.9"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BodyTextBlock" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E5E7EB"/>
            <Setter Property="FontFamily" Value="Bahnschrift Light"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontStyle" Value="Italic"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
            <Setter Property="TextOptions.TextRenderingMode" Value="Auto"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="1" BlurRadius="6" Opacity="0.8"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="FontFamily" Value="Bahnschrift Light"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BackButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#6366F1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4F46E5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ChangeFolderButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#8B5CF6"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#7C3AED"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="RefreshButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#22C55E"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#16A34A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CompressAllButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#F59E0B"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D97706"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#EF4444"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#DC2626"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PhotoCard" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1.03" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1.03" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border CornerRadius="16" Background="#0F172A">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.5" Color="#000000"/>
        </Border.Effect>

        <Grid>
            <!-- HEADER -->
            <Grid VerticalAlignment="Top" Margin="30,20,30,0" Height="80">
                <StackPanel>
                    <TextBlock x:Name="TitleText" Text="🖼️ Gallery Manager" Style="{StaticResource TitleTextBlock}" FontSize="28"/>
                    <TextBlock x:Name="SubtitleText" Text="Manage your Sims 4 screenshots" Style="{StaticResource BodyTextBlock}" FontSize="14" Margin="0,5,0,0"/>
                </StackPanel>

                <!-- BOTÓN CERRAR -->
                <Button x:Name="CloseButton"
                        Content="✕" 
                        HorizontalAlignment="Right" 
                        VerticalAlignment="Top"
                        Width="32"
                        Height="32"
                        Style="{StaticResource CloseButtonStyle}"
                        FontSize="16"
                        Click="CloseButton_Click"/>
            </Grid>

            <!-- TOOLBAR -->
            <Border Background="#1E293B" CornerRadius="12" Margin="30,110,30,0" Height="60" VerticalAlignment="Top">
                <Grid Margin="20,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock x:Name="FolderPathText" 
                                   Text="📁 Loading..." 
                                   Style="{StaticResource BodyTextBlock}" 
                                   FontSize="11" 
                                   VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis"
                                   MaxWidth="400"/>
                        <Button x:Name="ChangeFolderButton"
                                Content="Change Folder"
                                Style="{StaticResource ChangeFolderButtonStyle}"
                                FontSize="11"
                                Margin="15,0,0,0"
                                Click="ChangeFolderButton_Click"/>
                        <Button x:Name="BackToRootButton"
                                Content="⬅️ Back"
                                Style="{StaticResource BackButtonStyle}"
                                FontSize="11"
                                Margin="10,0,0,0"
                                Visibility="Collapsed"
                                Click="BackToRootButton_Click"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock x:Name="PhotoCountText" Text="0 photos" Style="{StaticResource BodyTextBlock}" FontSize="11" VerticalAlignment="Center" Margin="0,0,15,0"/>
                        <Button x:Name="CreateAlbumButton"
                                Content="📁 Create Album"
                                Style="{StaticResource ChangeFolderButtonStyle}"
                                FontSize="11"
                                Margin="0,0,10,0"
                                Click="CreateAlbumButton_Click"/>
                        <Button x:Name="RefreshButton"
                                Content="🔄 Refresh"
                                Style="{StaticResource RefreshButtonStyle}"
                                FontSize="11"
                                Margin="0,0,10,0"
                                Click="RefreshButton_Click"/>
                        <Button x:Name="CompressAllButton"
                                Content="📦 Compress All"
                                Style="{StaticResource CompressAllButtonStyle}"
                                FontSize="11"
                                Click="CompressAllButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- GRID DE FOTOS -->
            <ScrollViewer Margin="30,185,30,30" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <Grid x:Name="PhotosContainer">
                    <!-- Loading Indicator -->
                    <StackPanel x:Name="LoadingPanel" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Visible">
                        <TextBlock Text="⏳" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock x:Name="LoadingText" Text="Loading screenshots..." Style="{StaticResource TitleTextBlock}" FontSize="16" HorizontalAlignment="Center"/>
                    </StackPanel>

                    <!-- Empty State -->
                    <StackPanel x:Name="EmptyPanel" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
                        <TextBlock Text="📷" FontSize="64" HorizontalAlignment="Center" Margin="0,0,0,15" Opacity="0.5"/>
                        <TextBlock x:Name="EmptyText" Text="No screenshots found" Style="{StaticResource TitleTextBlock}" FontSize="18" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="EmptySubText" Text="Take some screenshots in The Sims 4 with 'C' key" Style="{StaticResource BodyTextBlock}" FontSize="12" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                        <TextBlock x:Name="HowToTakeScreenshotsText" 
                                   Text="How to take Screenshots" 
                                   Style="{StaticResource BodyTextBlock}" 
                                   FontSize="13" 
                                   HorizontalAlignment="Center" 
                                   Margin="0,15,0,0" 
                                   Foreground="#6366F1" 
                                   TextDecorations="Underline" 
                                   Cursor="Hand" 
                                   MouseLeftButtonUp="HowToTakeScreenshots_Click"/>
                    </StackPanel>

                    <!-- Photos Grid -->
                    <WrapPanel x:Name="PhotosGrid" Visibility="Collapsed" HorizontalAlignment="Left"/>
                </Grid>
            </ScrollViewer>

            <!-- REPOSITORY LINK -->
            <TextBlock x:Name="RepositoryLink"
                       Text="See Compressor Source and Repository"
                       FontFamily="Bahnschrift Light"
                       FontSize="10"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Bottom"
                       Margin="0,0,0,8"
                       Foreground="#6B7280"
                       TextDecorations="Underline"
                       Cursor="Hand"
                       Opacity="0.7"
                       MouseLeftButtonUp="RepositoryLink_Click">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="#6B7280"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#9CA3AF"/>
                                <Setter Property="Opacity" Value="1"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <!-- OVERLAY PARA VER FOTO -->
            <Grid x:Name="PhotoViewerOverlay" Background="#E6000000" Visibility="Collapsed" MouseLeftButtonDown="PhotoViewerOverlay_MouseDown">
                <Border CornerRadius="16" Background="#0F172A" MaxWidth="900" MaxHeight="700" Margin="50" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Grid>
                        <!-- Close Button -->
                        <Button Content="✕" 
                                Width="32" 
                                Height="32" 
                                HorizontalAlignment="Right" 
                                VerticalAlignment="Top" 
                                Margin="15"
                                FontSize="16"
                                Style="{StaticResource CloseButtonStyle}"
                                Click="ClosePhotoViewer_Click"
                                Panel.ZIndex="10"/>

                        <!-- Image -->
                        <Image x:Name="ViewerImage" Stretch="Uniform" Margin="20,60,20,80"/>

                        <!-- Info Bar -->
                        <Border Background="#1E293B" CornerRadius="12" Height="50" VerticalAlignment="Bottom" Margin="20,0,20,20">
                            <Grid Margin="15,0">
                                <TextBlock x:Name="ViewerFileName" Text="" Style="{StaticResource BodyTextBlock}" FontSize="12" VerticalAlignment="Center"/>
                                <TextBlock x:Name="ViewerFileSize" Text="" Style="{StaticResource BodyTextBlock}" FontSize="11" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                            </Grid>
                        </Border>
                    </Grid>
                </Border>
            </Grid>

            <!-- PROGRESS OVERLAY -->
            <Grid x:Name="ProgressOverlay" Background="#BF020617" Visibility="Collapsed">
                <Border CornerRadius="16" Background="#1E293B" Width="400" Height="150" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="30">
                    <StackPanel>
                        <TextBlock x:Name="ProgressTitle" Text="Processing..." Style="{StaticResource TitleTextBlock}" FontSize="18" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                        <ProgressBar x:Name="ProgressBar" Height="8" Background="#334155" Foreground="#22C55E" Minimum="0" Maximum="100" Value="0"/>
                        <TextBlock x:Name="ProgressText" Text="0%" Style="{StaticResource BodyTextBlock}" FontSize="12" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>