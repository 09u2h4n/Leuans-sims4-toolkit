<Window x:Class="ModernDesign.MVVM.View.AvatarSelectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Choose Avatar"
        Width="450" Height="300"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Border Background="#1E293B" CornerRadius="12" Padding="20">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Grid.Row="0" Text="👤 Choose Your Avatar" 
                       FontSize="18" FontWeight="Bold" Foreground="White" 
                       FontFamily="Bahnschrift Light" Margin="0,0,0,20"/>

            <!-- Avatares -->
            <WrapPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border x:Name="Avatar1" Tag="👤" Width="80" Height="80" CornerRadius="40" Margin="10" Cursor="Hand" MouseLeftButtonUp="Avatar_Click">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#22D3EE" Offset="0"/>
                            <GradientStop Color="#21b96b" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <TextBlock Text="👤" FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <Border x:Name="Avatar2" Tag="😎" Width="80" Height="80" CornerRadius="40" Margin="10" Cursor="Hand" MouseLeftButtonUp="Avatar_Click">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#F59E0B" Offset="0"/>
                            <GradientStop Color="#EF4444" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <TextBlock Text="😎" FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <Border x:Name="Avatar3" Tag="🎮" Width="80" Height="80" CornerRadius="40" Margin="10" Cursor="Hand" MouseLeftButtonUp="Avatar_Click">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#8B5CF6" Offset="0"/>
                            <GradientStop Color="#EC4899" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <TextBlock Text="🎮" FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <Border x:Name="Avatar4" Tag="🚀" Width="80" Height="80" CornerRadius="40" Margin="10" Cursor="Hand" MouseLeftButtonUp="Avatar_Click">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#6366F1" Offset="0"/>
                            <GradientStop Color="#22D3EE" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <TextBlock Text="🚀" FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <Border x:Name="Avatar5" Tag="💎" Width="80" Height="80" CornerRadius="40" Margin="10" Cursor="Hand" MouseLeftButtonUp="Avatar_Click">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#14B8A6" Offset="0"/>
                            <GradientStop Color="#3B82F6" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <TextBlock Text="💎" FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
            </WrapPanel>

            <!-- Buttons -->
            <Button Grid.Row="2" x:Name="CancelButton" Content="Cancel" Padding="15,8" 
                    HorizontalAlignment="Center" Margin="0,20,0,0"
                    Background="#334155" Foreground="White" BorderThickness="0" Cursor="Hand" Click="CancelButton_Click"/>
        </Grid>
    </Border>
</Window>